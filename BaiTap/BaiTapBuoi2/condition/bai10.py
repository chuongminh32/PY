# numbers = input("Nhap 5 so: ")
# array = [int(x) for x in numbers.split()] 
# zeroCount = 0
# positiveCount = 0
# negativeCount = 0
# for num in array :
#     if num == 0 : zeroCount += 1
#     elif num > 0 : positiveCount += 1
#     else : negativeCount += 1

# print(f"Co {positiveCount} so duong , {negativeCount} so am: va {zeroCount} so 0:")


# # cach 2 
# numbers = []

# # for i in range(5) :
# #     N = float(input(f"Nhap so thu {i+1}: "))
# #     numbers.append(N)

# # negativeCount = 0
# # positiveCount = 0
# # zeroCount = 0

# # for num in numbers :
# #     if num == 0 : zeroCount += 1
# #     elif num > 0 : positiveCount += 1
# #     else : negativeCount += 1

# # print(f"Co {zeroCount} so 0, {positiveCount} so duong, {negativeCount} so am")
